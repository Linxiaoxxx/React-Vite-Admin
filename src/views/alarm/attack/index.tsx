import { FC } from "react";

const AlarmAttack: FC = () => {
  return (
    <div className="flex flex-col w-full h-full overflow-hidden">
      alarm attack
    </div>
  );
};

export default AlarmAttack;
